#let setup-chinese() = {
  set text(
    font: ("PingFang SC", "Songti SC", "Noto Serif CJK SC"),
    size: 11pt,
    lang: "zh",
    region: "cn",
  )
  set par(
    justify: true,
    leading: 0.9em,
    first-line-indent: 2em,
  )
}
